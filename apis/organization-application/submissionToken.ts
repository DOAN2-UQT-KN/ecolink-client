/**
 * Header that carries proof the visitor owns the contact mailbox. The application form has no
 * login, so this token is the only credential the submit endpoints accept.
 */
export const SUBMISSION_TOKEN_HEADER = "x-submission-token";
